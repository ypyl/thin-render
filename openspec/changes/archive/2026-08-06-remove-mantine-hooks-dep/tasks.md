## 1. Remove the dependency

^- [x] 1.1 Run `npm uninstall @mantine/hooks` in `demo/` and verify `demo/package.json` and `demo/package-lock.json` no longer reference it

## 2. Verify

^- [x] 2.1 Build the demo (`npm run build` in `demo/`) — succeeds without the dependency
